dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
    // NOTHING ELSE HERE - no versionCatalogs!
}

rootProject.name = "build-logic"
include(":convention")
